require("arrow").setup {
  show_icons = true,
  -- NOTE: M key for file list
  leader_key = "M", -- Recommended to be a single key, to save files
  -- NOTE: m key for saved lines list
  buffer_leader_key = "m", -- Per Buffer Mappings, to save lines
}
